piVersion: couchbase.com/v1
kind: CouchbaseCluster
metadata:
  name: ${ACRONIMO}
spec:
  baseImage: registry.connect.redhat.com/couchbase/server
  version: 6.0.1-1
  authSecret: cb-example-auth
  exposeAdminConsole: true
  storage: ${STORAGE_DEV}
